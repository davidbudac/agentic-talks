A warm coral-wash aside with a heavy left border — reserves attention for one important caveat or failure mode.

```jsx
<Callout title="Most common failure mode">
  The subagent can't see prior turns — unless the parent bakes it into the task string.
</Callout>
```

- Use sparingly: one per slide at most. Without a `title` it reads as a quiet highlighted note.
- Lives on light surfaces; the coral-soft ground assumes a paper background behind it.
