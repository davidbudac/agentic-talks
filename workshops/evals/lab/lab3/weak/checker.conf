# The weak checker: "it compiles and the new test passes".
# TEST_FILTER is passed to Surefire as -Dtest=...; empty means run every test.
# RUN_OWN_TESTS=1 also runs the repo's own test suite (src/test/java).
TEST_FILTER=WeakCurrencyColumnTest
RUN_OWN_TESTS=0
