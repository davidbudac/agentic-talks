"""Invoice fixtures toolkit used across talks 02-07.

Modules: money (parsing and rounding), invoice (calculation), customers
(Unicode names), dates (issue dates and terms), fixtures (CSV loading),
export (CSV export), report (plain-text totals) and cli (the command line).
Standard library only.
"""
__version__ = "0.4.0"
